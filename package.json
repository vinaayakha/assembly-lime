{
  "name": "assembly-lime",
  "version": "0.0.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter '@assembly-lime/api' dev",
    "dev:web": "bun run --filter '@assembly-lime/web' dev",
    "dev:all": "bun run --filter '*' dev"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
